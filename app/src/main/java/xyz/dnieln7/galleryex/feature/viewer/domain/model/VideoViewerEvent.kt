package xyz.dnieln7.galleryex.feature.viewer.domain.model

/**
 * One-time side-effect commands for the video viewer screen.
 *
 * No events are defined yet; the sealed interface is scaffolded so that the
 * event channel infrastructure is in place for future additions.
 */
sealed interface VideoViewerEvent
